---
phase: 05-event-creation-enhancements
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - __tests__/components/VenueAutocompleteInput.test.tsx
  - __tests__/components/SharingSection.test.tsx
autonomous: true
requirements:
  - EVT-01
  - EVT-02
  - EVT-04
  - EVT-05
  - EVT-06
must_haves:
  truths:
    - "Test stub files exist before implementation begins"
    - "VenueAutocompleteInput test file has pending test cases for venue search and selection"
    - "SharingSection test file has pending test cases for registration link, QR code, and share buttons"
  artifacts:
    - path: "__tests__/components/VenueAutocompleteInput.test.tsx"
      provides: "Test stubs for EVT-01 (venue autocomplete) and EVT-02 (venue data persistence)"
      contains: "VenueAutocompleteInput"
    - path: "__tests__/components/SharingSection.test.tsx"
      provides: "Test stubs for EVT-04 (registration link), EVT-05 (QR download), EVT-06 (slug routing)"
      contains: "registration"
  key_links: []
---

<objective>
Create Wave 0 test stub files required by VALIDATION.md before any implementation begins.

Purpose: Nyquist compliance -- test files must exist so subsequent plans can run automated verification.
Output: Two test stub files with pending test cases covering EVT-01 through EVT-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-event-creation-enhancements/05-CONTEXT.md
@.planning/phases/05-event-creation-enhancements/05-RESEARCH.md
@.planning/phases/05-event-creation-enhancements/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test stub files</name>
  <files>__tests__/components/VenueAutocompleteInput.test.tsx, __tests__/components/SharingSection.test.tsx</files>
  <read_first>
    - vitest.config.ts (verify test configuration and path aliases)
    - __tests__ directory listing (check existing test patterns)
  </read_first>
  <action>
**1. Create `__tests__/components/VenueAutocompleteInput.test.tsx`:**

```typescript
import { describe, it, expect } from 'vitest'

describe('VenueAutocompleteInput', () => {
  // EVT-01: Admin can search for a venue via Google Maps autocomplete
  it.todo('calls /api/maps/autocomplete when input >= 3 chars')
  it.todo('does NOT call API when input < 3 chars')
  it.todo('renders dropdown with predictions from API response')
  it.todo('debounces API calls (300ms)')

  // EVT-02: Venue lat/lng/address/place_id saved on selection
  it.todo('calls /api/maps/details on prediction selection')
  it.todo('calls onVenueSelect with name, address, lat, lng, place_id')
  it.todo('calls onLocationChange on every keystroke (freetext fallback)')
  it.todo('shows "Venue saved" chip when selectedPlaceId is truthy')
})
```

**2. Create `__tests__/components/SharingSection.test.tsx`:**

```typescript
import { describe, it, expect } from 'vitest'

describe('Sharing Tab', () => {
  // EVT-04: System generates a unique registration link per event
  it.todo('constructs registration URL as NEXT_PUBLIC_PUBLIC_RESULTS_URL/e/[slug]/register')
  it.todo('displays registration URL in a read-only input')
  it.todo('copies registration URL to clipboard on button click')

  // EVT-05: QR code download
  it.todo('renders QRCodeSVG with registration URL as value')
  it.todo('renders hidden QRCodeCanvas for PNG export')
  it.todo('preview modal opens on PREVIEW button click')

  // EVT-06: Registration link includes event slug for direct routing
  it.todo('registration URL contains event.slug segment')
  it.todo('mailto href contains encoded registration URL')
  it.todo('sms href contains encoded registration URL')

  // D-08: Draft gate
  it.todo('shows "Registration link available after publishing" when status !== active')
  it.todo('shows sharing content when status === active')
})
```

Both files use `it.todo()` for pending tests -- these will be fleshed out as implementation proceeds. The stubs establish the test contract.
  </action>
  <verify>
    <automated>cd D:/Claude/leagueops && test -f __tests__/components/VenueAutocompleteInput.test.tsx && test -f __tests__/components/SharingSection.test.tsx && grep -q "VenueAutocompleteInput" __tests__/components/VenueAutocompleteInput.test.tsx && grep -q "registration" __tests__/components/SharingSection.test.tsx && npx vitest run --reporter=verbose 2>&1 | tail -10 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Both test stub files exist with pending test cases covering EVT-01 through EVT-06. Vitest suite runs without errors (todo tests are skipped, not failed).</done>
</task>

</tasks>

<verification>
- `test -f __tests__/components/VenueAutocompleteInput.test.tsx` -- file exists
- `test -f __tests__/components/SharingSection.test.tsx` -- file exists
- `npx vitest run` -- suite passes (todo tests skipped)
</verification>

<success_criteria>
1. VenueAutocompleteInput.test.tsx exists with todo stubs for EVT-01, EVT-02
2. SharingSection.test.tsx exists with todo stubs for EVT-04, EVT-05, EVT-06
3. Vitest suite runs green (all todos are skipped, not failed)
</success_criteria>

<output>
After completion, create `.planning/phases/05-event-creation-enhancements/05-00-SUMMARY.md`
</output>
