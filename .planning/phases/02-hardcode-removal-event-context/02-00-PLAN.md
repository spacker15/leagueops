---
phase: 02-hardcode-removal-event-context
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - __tests__/lib/store.test.tsx
autonomous: true
requirements:
  - SEC-04
  - SEC-05

must_haves:
  truths:
    - 'Store test file exists and runs green before any store changes'
    - 'Tests define expected behaviors for loadAll re-fire and realtime scoping'
  artifacts:
    - path: '__tests__/lib/store.test.tsx'
      provides: 'Behavioral test scaffold for SEC-04 and SEC-05'
      min_lines: 80
  key_links:
    - from: '__tests__/lib/store.test.tsx'
      to: 'lib/store.tsx'
      via: 'import of AppProvider'
      pattern: 'import.*AppProvider.*store'
---

<objective>
Create behavioral test scaffolding for `lib/store.tsx` covering SEC-04 (loadAll re-fires when eventId changes) and SEC-05 (realtime subscription filter and channel teardown on event switch). These tests must exist before the store is modified in Plan 03.

Purpose: Nyquist requirement -- tests define expected behavior before implementation changes.
Output: `__tests__/lib/store.test.tsx` with failing tests that Plan 03 will make pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hardcode-removal-event-context/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create store behavioral test scaffold</name>
  <files>__tests__/lib/store.test.tsx</files>
  <read_first>
    - lib/store.tsx (current implementation -- understand AppProvider signature, loadAll, realtime setup, useCallback deps)
    - __tests__/lib/engines/_mockSb.ts (existing mock pattern for Supabase client)
    - vitest.config.ts (test config -- path aliases, setup file)
    - vitest.setup.ts (existing test setup)
  </read_first>
  <action>
Create `__tests__/lib/store.test.tsx` with the following behavioral tests. Use React Testing Library to render `AppProvider` and assert behaviors.

**Mock setup:**

- Mock `@/lib/db` module -- all functions return empty arrays/null (loadAll must succeed without real DB)
- Mock `@/supabase/client` -- return a mock Supabase client with `.channel()`, `.on()`, `.subscribe()`, `.removeChannel()` methods that are `vi.fn()` stubs
- The mock `channel()` should return an object with chainable `.on()` and `.subscribe()` methods
- `subscribe()` should return the channel object itself (for cleanup)

**Test cases (these should initially FAIL because current store has broken dep arrays):**

1. **SEC-04: loadAll re-fires when eventId changes**
   - Render `AppProvider` with `eventId={1}`, wait for loadAll to complete (db.getEvent called with 1)
   - Rerender with `eventId={2}`
   - Assert `db.getEvent` was called again with `2`
   - This will FAIL because current dep array is `[]`

2. **SEC-05: realtime subscription includes event_id filter**
   - Render `AppProvider` with `eventId={1}`
   - Assert `sb.channel('leagueops-realtime').on()` was called with an object containing `filter: 'event_id=eq.1'`
   - This will FAIL because current realtime setup has no filter

3. **SEC-05: realtime channel torn down on eventId change**
   - Render `AppProvider` with `eventId={1}`
   - Capture the channel reference
   - Rerender with `eventId={2}`
   - Assert `sb.removeChannel` was called with the old channel
   - Assert a new `sb.channel()` call was made
   - This will FAIL because current dep array is `[currentDate]` only

4. **SEC-04: null guard -- loadAll does not fire when eventId is undefined**
   - Render `AppProvider` with `eventId={undefined}`
   - Assert `db.getEvent` was NOT called
   - This may pass or fail depending on current guard -- either outcome is acceptable for scaffold

**Test helper:** Create a simple wrapper component that renders `AppProvider` and exposes a `useApp()` consumer for assertion:

```typescript
function TestConsumer() {
  const { eventId } = useApp()
  return <div data-testid="event-id">{eventId}</div>
}
```

Mark tests that are expected to fail with a `// EXPECTED FAIL: Plan 03 fixes this` comment but do NOT use `.skip` or `.todo` -- they must actually run and fail so Plan 03 can verify the fix makes them pass.

IMPORTANT: Since these tests will fail initially, use `test.fails()` (Vitest's built-in) for the 3 tests expected to fail. This way the test suite stays green in Wave 0 while clearly documenting expected failures. Plan 03 will convert them to regular `test()` calls.
</action>
<verify>
<automated>cd C:/Users/Seanp/OneDrive/Documents/Claude/leagueops && npx vitest run **tests**/lib/store.test.tsx 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria> - File `__tests__/lib/store.test.tsx` exists and contains `import { AppProvider` or `import { useApp` - File contains at least 4 `test(` or `test.fails(` calls - File contains string `event_id=eq.1` (realtime filter assertion) - File contains string `SEC-04` and `SEC-05` in test names or comments - `npx vitest run __tests__/lib/store.test.tsx` exits 0 (test.fails tests pass by failing, regular tests pass normally) - File contains mock of `@/lib/db` with vi.mock - File contains mock of `@/supabase/client` with vi.mock
</acceptance_criteria>
<done>Store behavioral test file runs green with test.fails markers. Plan 03 will flip these to regular tests after fixing the store.</done>
</task>

</tasks>

<verification>
- `npx vitest run __tests__/lib/store.test.tsx` exits 0
- File contains behavioral tests for both SEC-04 and SEC-05
</verification>

<success_criteria>

- `__tests__/lib/store.test.tsx` exists with 4+ test cases
- Test suite runs green (using test.fails for expected failures)
- Tests cover: loadAll re-fire, realtime filter, channel teardown, null guard
  </success_criteria>

<output>
After completion, create `.planning/phases/02-hardcode-removal-event-context/02-00-SUMMARY.md`
</output>
