---
phase: 02-hardcode-removal-event-context
plan: '02'
type: execute
wave: 2
depends_on: ['02-01']
files_modified:
  - app/api/referee-engine/route.ts
  - app/api/field-engine/route.ts
  - app/api/weather-engine/route.ts
  - app/api/eligibility/route.ts
  - app/api/lightning/route.ts
  - app/api/conflicts/route.ts
  - app/api/fields/route.ts
  - app/api/incidents/route.ts
  - app/api/medical/route.ts
  - app/api/ops-log/route.ts
  - app/api/referees/route.ts
  - app/api/registration-fees/route.ts
  - app/api/rules/route.ts
  - app/api/rules/changes/route.ts
  - app/api/team-payments/route.ts
  - app/api/teams/route.ts
  - app/api/volunteers/route.ts
  - app/api/weather/route.ts
  - app/api/admin/create-user/route.ts
autonomous: true
requirements: [SEC-04]
must_haves:
  truths:
    - "No API route contains `?? '1'` or `?? 1` as an event_id fallback"
    - 'No API route contains a literal `event_id: 1` insert'
    - 'API routes that require event_id return 400 if the parameter is missing'
    - 'Engine-calling routes pass eventId to the updated engine function signatures'
  artifacts:
    - path: 'app/api/conflicts/route.ts'
      provides: 'Event-scoped conflicts route with 400 guard'
      contains: 'if (!eventId)'
    - path: 'app/api/lightning/route.ts'
      provides: 'Lightning route with dynamic event_id'
      contains: 'event_id: eventId'
    - path: 'app/api/rules/route.ts'
      provides: 'Rules route with dynamic event_id inserts'
      contains: 'event_id: eventId'
  key_links:
    - from: 'app/api/referee-engine/route.ts'
      to: 'lib/engines/referee.ts'
      via: 'passes eventId from searchParams to runRefereeEngine'
      pattern: 'runRefereeEngine.*eventId'
    - from: 'app/api/field-engine/route.ts'
      to: 'lib/engines/field.ts'
      via: 'passes eventId from searchParams to runFieldConflictEngine'
      pattern: 'runFieldConflictEngine.*eventId'
---

<objective>
Replace all `?? '1'` fallbacks and literal `event_id: 1` in API routes with dynamic event_id from request params, and update engine-calling routes to pass eventId to the new engine signatures from Plan 01.

Purpose: API routes are the bridge between client components and engines. After engines accept eventId (Plan 01), routes must parse it from the request and forward it. Per D-02, no constant fallbacks allowed.

Output: All ~19 API routes use dynamic event_id. Missing event_id returns 400.
</objective>

<execution_context>
@C:/Users/Seanp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Seanp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hardcode-removal-event-context/02-CONTEXT.md
@.planning/phases/02-hardcode-removal-event-context/02-RESEARCH.md
@.planning/phases/02-hardcode-removal-event-context/02-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, engine signatures are: -->

lib/engines/referee.ts:
runRefereeEngine(eventDateId: number, eventId: number, sb: SupabaseClient)
findAvailableRefs(gameId: number, eventDateId: number, eventId: number, sb: SupabaseClient)

lib/engines/weather.ts:
runWeatherEngine(complexId: number, eventId: number, apiKey: string, sb: SupabaseClient)
liftLightningDelay(complexId: number, eventId: number, sb: SupabaseClient)

lib/engines/field.ts:
runFieldConflictEngine(eventDateId: number, eventId: number, sb: SupabaseClient)
runFullConflictScan(eventDateId: number, eventId: number, sb: SupabaseClient)

lib/engines/unified.ts:
runUnifiedEngine(eventDateId: number, eventId: number, sb: SupabaseClient)
resolveAlert(alertId: number, resolvedBy: string, eventId: number, sb: SupabaseClient)

lib/engines/rules.ts:
loadRules(eventId: number, sb: SupabaseClient)
getRules(category: string, eventId: number, sb: SupabaseClient) -- all rule functions now take eventId
resetAllRules(eventId: number, sb: SupabaseClient)
updateRule(id: number, value: any, eventId: number, sb: SupabaseClient)

<!-- API route pattern — two categories: -->

Category A: Routes using searchParams.get('event_id') ?? '1'

- conflicts, fields, eligibility, field-engine, incidents, medical, ops-log, referees,
  registration-fees, rules, rules/changes, team-payments, teams, volunteers, weather

Category B: Routes using event_id from request body with ?? 1 fallback

- lightning (5 occurrences of event_id ?? 1 in body)
- admin/create-user (event_id ?? 1 in body)
- rules (event_id: 1 in insert literals, event_id = 1 default in POST body)
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Category A data routes — replace ?? '1' with 400 guard</name>
  <read_first>
    - app/api/conflicts/route.ts
    - app/api/fields/route.ts
    - app/api/eligibility/route.ts
    - app/api/incidents/route.ts
    - app/api/medical/route.ts
    - app/api/ops-log/route.ts
    - app/api/referees/route.ts
    - app/api/registration-fees/route.ts
    - app/api/team-payments/route.ts
    - app/api/teams/route.ts
    - app/api/volunteers/route.ts
    - app/api/weather/route.ts
    - app/api/rules/route.ts
    - app/api/rules/changes/route.ts
  </read_first>
  <files>
    app/api/conflicts/route.ts, app/api/fields/route.ts, app/api/eligibility/route.ts,
    app/api/incidents/route.ts, app/api/medical/route.ts,
    app/api/ops-log/route.ts, app/api/referees/route.ts, app/api/registration-fees/route.ts,
    app/api/team-payments/route.ts, app/api/teams/route.ts, app/api/volunteers/route.ts,
    app/api/weather/route.ts, app/api/rules/route.ts, app/api/rules/changes/route.ts
  </files>
  <action>
For each of the 14 data route files listed above, apply this identical transformation:

**Before (line ~7 in most files):**

```typescript
const eventId = searchParams.get('event_id') ?? '1'
```

**After:**

```typescript
const eventId = searchParams.get('event_id')
if (!eventId) return NextResponse.json({ error: 'event_id required' }, { status: 400 })
```

This applies to every handler function in each route file (GET, POST, PATCH, DELETE).

**For app/api/rules/route.ts POST handler (additional changes beyond the guard):**

- Replace `event_id: 1` literal inserts (lines 60, 70) with `event_id: Number(eventId)`
- Replace `const { action, id, event_id = 1 } = body` (line 83) with `const { action, id } = body` and use `Number(eventId)` from searchParams
- Pass `Number(eventId)` to all `loadRules`, `resetAllRules`, `updateRule` calls

**Exception routes that do NOT need event_id guard:**

- `app/api/join/route.ts` — public, no event_id param
- `app/api/checkins/route.ts` — public check-in
- `app/api/auth/check-email/route.ts` — public
- `app/api/auth/program-prefill/route.ts` — already has auth
  These files are NOT modified in this task.
  </action>
  <verify>
  <automated>grep -rn "?? '1'\|?? 1\|event_id: 1\|event_id = 1" app/api/conflicts/route.ts app/api/fields/route.ts app/api/eligibility/route.ts app/api/incidents/route.ts app/api/medical/route.ts app/api/ops-log/route.ts app/api/referees/route.ts app/api/registration-fees/route.ts app/api/team-payments/route.ts app/api/teams/route.ts app/api/volunteers/route.ts app/api/weather/route.ts app/api/rules/route.ts app/api/rules/changes/route.ts | grep -v "eventId\|event_id: eventId\|event_id: Number" || echo "PASS: No hardcoded event_id in data routes"</automated>
  </verify>
  <acceptance_criteria> - All 14 data route files contain `if (!eventId) return NextResponse.json({ error: 'event_id required' }, { status: 400 })` - None of the 14 files contain `?? '1'` - `app/api/rules/route.ts` contains `event_id: Number(eventId)` instead of `event_id: 1`
  </acceptance_criteria>
  <done>All 14 Category A data routes require event_id parameter with 400 guard, no fallback to '1'</done>
  </task>

<task type="auto">
  <name>Task 2: Fix engine-calling routes — pass eventId to updated engine signatures</name>
  <read_first>
    - app/api/referee-engine/route.ts
    - app/api/weather-engine/route.ts
    - app/api/field-engine/route.ts
  </read_first>
  <files>app/api/referee-engine/route.ts, app/api/weather-engine/route.ts, app/api/field-engine/route.ts</files>
  <action>
These three routes call engine functions whose signatures changed in Plan 01 to include `eventId: number`. Update the routes to extract event_id from searchParams, add a 400 guard, and pass eventId to the engine calls.

**app/api/referee-engine/route.ts:**

1. Replace `searchParams.get('event_id') ?? '1'` with `searchParams.get('event_id')` + 400 guard (same pattern as Task 1)
2. Update `runRefereeEngine(eventDateId, sb)` to `runRefereeEngine(eventDateId, Number(eventId), sb)`
3. Update `findAvailableRefs(gameId, eventDateId, sb)` to `findAvailableRefs(gameId, eventDateId, Number(eventId), sb)`

**app/api/weather-engine/route.ts:**

1. Add `event_id` extraction from searchParams if not already present: `const eventId = searchParams.get('event_id')`
2. Add 400 guard: `if (!eventId) return NextResponse.json({ error: 'event_id required' }, { status: 400 })`
3. Update `runWeatherEngine(complexId, apiKey, sb)` to `runWeatherEngine(complexId, Number(eventId), apiKey, sb)`

**app/api/field-engine/route.ts:**

1. Replace `searchParams.get('event_id') ?? '1'` with `searchParams.get('event_id')` + 400 guard
2. Update `runFieldConflictEngine(eventDateId, sb)` to `runFieldConflictEngine(eventDateId, Number(eventId), sb)`
3. Update `runFullConflictScan(eventDateId, sb)` to `runFullConflictScan(eventDateId, Number(eventId), sb)`
   </action>
   <verify>
   <automated>grep -rn "?? '1'\|?? 1" app/api/referee-engine/route.ts app/api/weather-engine/route.ts app/api/field-engine/route.ts || echo "PASS: No fallbacks in engine routes" && grep -n "Number(eventId)" app/api/referee-engine/route.ts app/api/weather-engine/route.ts app/api/field-engine/route.ts | head -10</automated>
   </verify>
   <acceptance_criteria> - `app/api/referee-engine/route.ts` calls `runRefereeEngine(eventDateId, Number(eventId), sb)` - `app/api/weather-engine/route.ts` calls `runWeatherEngine(complexId, Number(eventId), apiKey, sb)` - `app/api/field-engine/route.ts` calls `runFieldConflictEngine(eventDateId, Number(eventId), sb)` - All three files have 400 guard for missing event_id - No `?? '1'` fallbacks remain
   </acceptance_criteria>
   <done>All 3 engine-calling routes pass eventId to updated engine signatures and have 400 guards</done>
   </task>

<task type="auto">
  <name>Task 3: Fix Category B routes — lightning and admin/create-user body fallbacks</name>
  <read_first>
    - app/api/lightning/route.ts
    - app/api/admin/create-user/route.ts
  </read_first>
  <files>app/api/lightning/route.ts, app/api/admin/create-user/route.ts</files>
  <action>
**app/api/lightning/route.ts:**
This route extracts `event_id` from the request body (`const { complex_id, event_id, ... } = body`). It has 5 occurrences of `event_id ?? 1`.

1. Add a guard after body parsing: `if (!event_id) return NextResponse.json({ error: 'event_id required' }, { status: 400 })`
2. Replace all `event_id ?? 1` with `event_id` (now guaranteed to exist)
3. The `liftLightningDelay(Number(complex_id), Number(event_id ?? 1), sb)` call becomes `liftLightningDelay(Number(complex_id), Number(event_id), sb)`
4. All `.eq('event_id', event_id ?? 1)` become `.eq('event_id', event_id)`
5. All `event_id: event_id ?? 1` in insert objects become `event_id: event_id`

**app/api/admin/create-user/route.ts:**
This route has `event_id: event_id ?? 1` in user_roles inserts (lines 59, 71).

1. Add validation: if `event_id` is not in the request body, return 400
2. Replace `event_id: event_id ?? 1` with `event_id: event_id` in both insert statements
3. Keep the existing auth check (`auth.getUser()`) that is already present in this route

After both files, run `npm run type-check` and `npm run test` to verify no regressions.
</action>
<verify>
<automated>grep -rn "?? 1\|event_id: 1" app/api/lightning/route.ts app/api/admin/create-user/route.ts || echo "PASS: No hardcoded event_id in Category B routes" && npm run type-check</automated>
</verify>
<acceptance_criteria> - `app/api/lightning/route.ts` contains zero occurrences of `?? 1` - `app/api/lightning/route.ts` contains `if (!event_id) return NextResponse.json({ error: 'event_id required' }` - `app/api/admin/create-user/route.ts` contains zero occurrences of `?? 1` - `npm run type-check` passes with zero errors - `npm run test` passes
</acceptance_criteria>
<done>All API routes in the codebase use dynamic event_id with proper 400 guards. No ?? 1 or ?? '1' fallbacks remain in app/api/.</done>
</task>

</tasks>

<verification>
1. `grep -rn "?? '1'\|?? 1" app/api/ | grep -v node_modules | wc -l` returns 0
2. `grep -rn "event_id: 1\|event_id = 1" app/api/ | grep -v "eventId\|Number(eventId)\|Number(event_id)" | wc -l` returns 0
3. `npm run type-check` passes
4. `npm run test` passes
</verification>

<success_criteria>

- Zero `?? '1'` or `?? 1` event_id fallbacks in any API route
- Zero literal `event_id: 1` inserts in any API route
- All engine-calling routes pass eventId to the updated engine signatures
- Type-check and test suite pass
  </success_criteria>

<output>
After completion, create `.planning/phases/02-hardcode-removal-event-context/02-02-SUMMARY.md`
</output>
