---
phase: 02-hardcode-removal-event-context
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/engines/referee.ts
  - lib/engines/weather.ts
  - lib/engines/field.ts
  - lib/engines/unified.ts
  - lib/engines/rules.ts
autonomous: true
requirements: [SEC-04]
must_haves:
  truths:
    - 'No engine file contains `const EVENT_ID = 1`'
    - "No engine file contains a literal `event_id: 1` or `.eq('event_id', 1)`"
    - 'Every engine entry-point function accepts `eventId: number` as a required parameter'
    - 'All internal usages of EVENT_ID replaced with the eventId parameter'
  artifacts:
    - path: 'lib/engines/referee.ts'
      provides: 'Referee engine with eventId parameter'
      contains: 'eventId: number'
    - path: 'lib/engines/weather.ts'
      provides: 'Weather engine with eventId parameter'
      contains: 'eventId: number'
    - path: 'lib/engines/field.ts'
      provides: 'Field engine with eventId parameter'
      contains: 'eventId: number'
    - path: 'lib/engines/unified.ts'
      provides: 'Unified engine with eventId parameter — no literal event_id: 1'
      contains: 'eventId: number'
    - path: 'lib/engines/rules.ts'
      provides: 'Rules engine with EVENT_ID constant removed'
      contains: 'eventId: number'
  key_links:
    - from: 'lib/engines/unified.ts'
      to: 'lib/engines/referee.ts'
      via: 'direct import — passes eventId through'
      pattern: 'runRefereeEngine.*eventId'
    - from: 'lib/engines/unified.ts'
      to: 'lib/engines/field.ts'
      via: 'direct import — passes eventId through'
      pattern: 'runFieldConflictEngine.*eventId'
---

<objective>
Replace all `const EVENT_ID = 1` constants and literal `event_id: 1` hardcodes in the five engine modules with an `eventId: number` function parameter.

Purpose: Engines are the foundation layer — API routes and components pass eventId DOWN to engines. Fixing engines first (per D-06) ensures the parameter exists when callers are updated in later plans.

Output: All engine entry-point functions accept `eventId: number`. No hardcoded event_id remains in `lib/engines/`.
</objective>

<execution_context>
@C:/Users/Seanp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Seanp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hardcode-removal-event-context/02-CONTEXT.md
@.planning/phases/02-hardcode-removal-event-context/02-RESEARCH.md
@.planning/phases/01-engine-client-refactor/01-01-SUMMARY.md

<interfaces>
<!-- Phase 1 established the sb: SupabaseClient injection pattern. Phase 2 adds eventId: number alongside it. -->

Current engine signatures (from Phase 1 refactor):

lib/engines/referee.ts:
export async function runRefereeEngine(eventDateId: number, sb: SupabaseClient)
export async function findAvailableRefs(gameId: number, eventDateId: number, sb: SupabaseClient)
// Also has: clearStaleConflicts(sb) — internal, uses EVENT_ID

lib/engines/weather.ts:
export async function runWeatherEngine(complexId: number, apiKey: string, sb: SupabaseClient)
export async function liftLightningDelay(complexId: number, eventId: number, sb: SupabaseClient)
// liftLightningDelay already has eventId! Other functions use EVENT_ID constant.

lib/engines/field.ts:
export async function runFieldConflictEngine(eventDateId: number, sb: SupabaseClient)
export async function applyResolution(conflictId: number, resolution: string, resolvedBy: string, sb: SupabaseClient)
export async function runFullConflictScan(eventDateId: number, sb: SupabaseClient)

lib/engines/unified.ts:
export async function runUnifiedEngine(eventDateId: number, sb: SupabaseClient)
export async function resolveAlert(alertId: number, resolvedBy: string, sb: SupabaseClient)
export async function generateShiftHandoff(createdBy: string, sb: SupabaseClient)

lib/engines/rules.ts:
const EVENT_ID = 1
export async function loadRules(eventId = EVENT_ID, sb: SupabaseClient)
export async function resetAllRules(eventId = EVENT_ID, sb: SupabaseClient)
// Other rule functions (getRules, getRule, getRuleNum, etc.) call loadRules internally
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add eventId parameter to referee, weather, and field engines</name>
  <read_first>
    - lib/engines/referee.ts
    - lib/engines/weather.ts
    - lib/engines/field.ts
  </read_first>
  <files>lib/engines/referee.ts, lib/engines/weather.ts, lib/engines/field.ts</files>
  <action>
For each of the three engine files, perform the same transformation:

**lib/engines/referee.ts:**

1. Delete `const EVENT_ID = 1` (line 25)
2. Add `eventId: number` parameter to `runRefereeEngine` — new signature: `runRefereeEngine(eventDateId: number, eventId: number, sb: SupabaseClient)`
3. Add `eventId: number` parameter to `findAvailableRefs` — new signature: `findAvailableRefs(gameId: number, eventDateId: number, eventId: number, sb: SupabaseClient)`
4. Add `eventId: number` to `clearStaleConflicts` — new signature: `clearStaleConflicts(eventId: number, sb: SupabaseClient)`
5. Replace every internal reference to `EVENT_ID` with `eventId` (all `.eq('event_id', EVENT_ID)` calls)

**lib/engines/weather.ts:**

1. Delete `const EVENT_ID = 1` (line 15)
2. Add `eventId: number` parameter to `runWeatherEngine` — new signature: `runWeatherEngine(complexId: number, eventId: number, apiKey: string, sb: SupabaseClient)`
3. Note: `liftLightningDelay` already accepts `eventId: number` — no change needed there
4. Replace every internal reference to `EVENT_ID` with `eventId`

**lib/engines/field.ts:**

1. Delete `const EVENT_ID = 1` (line 19)
2. Add `eventId: number` parameter to `runFieldConflictEngine` — new signature: `runFieldConflictEngine(eventDateId: number, eventId: number, sb: SupabaseClient)`
3. Add `eventId: number` to `applyResolution` — new signature: `applyResolution(conflictId: number, resolution: string, resolvedBy: string, eventId: number, sb: SupabaseClient)`
4. Add `eventId: number` to `runFullConflictScan` — new signature: `runFullConflictScan(eventDateId: number, eventId: number, sb: SupabaseClient)`
5. Replace every internal reference to `EVENT_ID` with `eventId`

Per D-02: Never replace with another constant fallback. The parameter is required (no default value).
</action>
<verify>
<automated>grep -rn "EVENT_ID\|event_id._: 1\|event_id._=.\*1" lib/engines/referee.ts lib/engines/weather.ts lib/engines/field.ts | grep -v "eventId" | grep -v "// " || echo "PASS: No hardcoded event_id remaining"</automated>
</verify>
<acceptance_criteria> - `lib/engines/referee.ts` does NOT contain `const EVENT_ID = 1` - `lib/engines/referee.ts` contains `eventId: number` in `runRefereeEngine` signature - `lib/engines/referee.ts` contains `eventId: number` in `findAvailableRefs` signature - `lib/engines/weather.ts` does NOT contain `const EVENT_ID = 1` - `lib/engines/weather.ts` contains `runWeatherEngine(complexId: number, eventId: number,` - `lib/engines/field.ts` does NOT contain `const EVENT_ID = 1` - `lib/engines/field.ts` contains `eventId: number` in `runFieldConflictEngine` signature - grep for `EVENT_ID` in all three files returns zero matches
</acceptance_criteria>
<done>All three engine files have eventId as a required parameter, no EVENT_ID constant, no literal event_id: 1</done>
</task>

<task type="auto">
  <name>Task 2: Add eventId parameter to unified and rules engines, update internal engine calls</name>
  <read_first>
    - lib/engines/unified.ts
    - lib/engines/rules.ts
  </read_first>
  <files>lib/engines/unified.ts, lib/engines/rules.ts</files>
  <action>
**lib/engines/unified.ts:**
1. Add `eventId: number` parameter to `runUnifiedEngine` — new signature: `runUnifiedEngine(eventDateId: number, eventId: number, sb: SupabaseClient)`
2. Add `eventId: number` parameter to `resolveAlert` — new signature: `resolveAlert(alertId: number, resolvedBy: string, eventId: number, sb: SupabaseClient)`
3. Add `eventId: number` parameter to `generateShiftHandoff` — new signature: `generateShiftHandoff(createdBy: string, eventId: number, sb: SupabaseClient)`
4. Replace ALL literal `event_id: 1` occurrences (lines ~74, 106, 127, 175, 195, 230, 343, 363, 415 per grep) with `event_id: eventId`
5. Replace ALL `.eq('event_id', 1)` with `.eq('event_id', eventId)`
6. Update internal calls to sub-engines to pass `eventId`:
   - `runRefereeEngine(eventDateId, eventId, sb)` (was `runRefereeEngine(eventDateId, sb)`)
   - `runFieldConflictEngine(eventDateId, eventId, sb)` (was `runFieldConflictEngine(eventDateId, sb)`)
   - Any `loadRules` calls: pass `eventId` as first argument

**lib/engines/rules.ts:**

1. Delete `const EVENT_ID = 1` (line 13)
2. Change `loadRules(eventId = EVENT_ID, sb)` to `loadRules(eventId: number, sb: SupabaseClient)` — remove default value, make eventId required
3. Change `resetAllRules(eventId = EVENT_ID, sb)` to `resetAllRules(eventId: number, sb: SupabaseClient)` — remove default value
4. Update internal `loadRules` callers inside rules.ts: `getRules`, `getRule`, `getRuleNum`, `getRuleBool`, `getWeatherThresholds`, `getRefereeRules`, `getSchedulingRules` — these call `loadRules()` without arguments; add `eventId: number` as first parameter to each and pass through to `loadRules(eventId, sb)`
5. Update `updateRule` to accept `eventId: number` and pass to `loadRules`
6. Key: every rules.ts function that calls `loadRules` must now receive `eventId` as a parameter and forward it

Per D-02: No default fallback values on eventId parameters.
</action>
<verify>
<automated>grep -rn "EVENT_ID\|event_id._: 1\|event_id._=.\*1\|= EVENT_ID" lib/engines/unified.ts lib/engines/rules.ts | grep -v "eventId" | grep -v "event_id: eventId" | grep -v "//" || echo "PASS: No hardcoded event_id remaining"</automated>
</verify>
<acceptance_criteria> - `lib/engines/unified.ts` does NOT contain any literal `event_id: 1` - `lib/engines/unified.ts` contains `runUnifiedEngine(eventDateId: number, eventId: number,` - `lib/engines/unified.ts` contains `resolveAlert(alertId: number, resolvedBy: string, eventId: number,` - `lib/engines/unified.ts` contains `event_id: eventId` (dynamic replacement) - `lib/engines/unified.ts` calls `runRefereeEngine(eventDateId, eventId, sb)` - `lib/engines/unified.ts` calls `runFieldConflictEngine(eventDateId, eventId, sb)` - `lib/engines/rules.ts` does NOT contain `const EVENT_ID = 1` - `lib/engines/rules.ts` does NOT contain `eventId = EVENT_ID` default - `lib/engines/rules.ts` `loadRules` signature is `loadRules(eventId: number, sb: SupabaseClient)` - `npm run type-check` passes (run after both tasks) - `npm run test` passes (existing engine tests updated to pass eventId)
</acceptance_criteria>
<done>All five engine modules accept eventId as a required parameter. No hardcoded event_id = 1 remains in lib/engines/. Type-check and tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -rn "EVENT_ID\|event_id.*: 1\|event_id.*=.*1" lib/engines/ | grep -v "eventId\|event_id: eventId\|// " | wc -l` returns 0
2. `npm run type-check` passes with zero errors
3. `npm run test` passes — existing engine tests in `__tests__/lib/engines/` updated to pass eventId parameter
</verification>

<success_criteria>

- Zero `const EVENT_ID = 1` constants remain in lib/engines/
- Zero literal `event_id: 1` or `.eq('event_id', 1)` remain in lib/engines/
- All engine entry-point functions have `eventId: number` as a required parameter
- Type-check and test suite pass
  </success_criteria>

<output>
After completion, create `.planning/phases/02-hardcode-removal-event-context/02-01-SUMMARY.md`
</output>
