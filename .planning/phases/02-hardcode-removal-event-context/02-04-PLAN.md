---
phase: 02-hardcode-removal-event-context
plan: '04'
type: execute
wave: 3
depends_on: ['02-02', '02-03']
files_modified:
  - components/engine/CommandCenter.tsx
  - components/engine/EngineTab.tsx
  - components/checkin/CheckInTab.tsx
  - components/incidents/IncidentsTab.tsx
  - components/schedule/ScheduleTab.tsx
  - components/refs/RefsTab.tsx
  - components/rules/RulesTab.tsx
  - components/settings/LeagueSettingsTab.tsx
  - components/weather/WeatherTab.tsx
  - components/payments/PaymentsTab.tsx
  - components/programs/ProgramApprovals.tsx
  - components/programs/ProgramDashboard.tsx
  - components/programs/RegistrationConfig.tsx
  - components/AppShell.tsx
  - components/auth/UserManagement.tsx
  - components/auth/RefereePortal.tsx
  - components/auth/VolunteerPortal.tsx
  - components/auth/RegisterPage.tsx
autonomous: true
requirements: [SEC-04, SEC-05]
must_haves:
  truths:
    - 'No component contains a literal `event_id: 1` or `event_id=1` in any query, insert, or URL'
    - 'No component contains `?? 1` as an eventId fallback'
    - 'No component contains `(state.event as any)?.id ?? 1` cast pattern'
    - 'Components inside AppProvider use `useApp().eventId` for event scoping'
    - 'Portal components outside AppProvider use `useAuth().userRole.event_id` for event scoping'
    - 'QR code URLs use event slug per D-05 pattern `/checkin/{eventSlug}/{token}`'
    - 'Switching events shows no stale data from the previous event'
  artifacts:
    - path: 'components/engine/CommandCenter.tsx'
      provides: 'Command center with dynamic eventId in queries and subscriptions'
      contains: 'useApp'
    - path: 'components/checkin/CheckInTab.tsx'
      provides: 'Check-in tab with slug-based QR URLs'
      contains: 'state.event?.slug'
    - path: 'components/auth/RefereePortal.tsx'
      provides: 'Referee portal using userRole.event_id'
      contains: 'userRole?.event_id'
  key_links:
    - from: 'components/engine/CommandCenter.tsx'
      to: 'useApp().eventId'
      via: 'destructured from useApp hook'
      pattern: 'const.*eventId.*=.*useApp'
    - from: 'components/checkin/CheckInTab.tsx'
      to: 'state.event?.slug'
      via: 'QR URL construction'
      pattern: 'checkin.*slug'
    - from: 'components/auth/RefereePortal.tsx'
      to: 'useAuth().userRole'
      via: 'event_id from user role (no AppProvider)'
      pattern: 'userRole.*event_id'
---

<objective>
Replace all hardcoded `event_id: 1`, `event_id=1`, `?? 1`, and `(state.event as any)?.id ?? 1` references across ~18 component files with dynamic eventId from `useApp()` or `useAuth()`.

Purpose: Components are the final layer (per D-06). With engines (Plan 01), API routes (Plan 02), and store (Plan 03) all using dynamic eventId, components must now send the correct eventId in their API calls, Supabase queries, and action payloads.

Output: Zero hardcoded event_id references remain in any component. QR codes use event slug (D-05).
</objective>

<execution_context>
@C:/Users/Seanp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Seanp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hardcode-removal-event-context/02-CONTEXT.md
@.planning/phases/02-hardcode-removal-event-context/02-RESEARCH.md
@.planning/phases/02-hardcode-removal-event-context/02-03-SUMMARY.md

<interfaces>
<!-- useApp() provides: -->
const { state, eventId, currentDate, todayGames, ...actions } = useApp()
// eventId: number (required, no default — from Plan 03)
// state.event?.slug: string — the event slug for QR URLs (D-05)

<!-- useAuth() provides: -->

const { userRole } = useAuth()
// userRole?.event_id: number — for portal components outside AppProvider

<!-- API routes now require event_id param (Plan 02): -->

// fetch('/api/field-engine?event_id=${eventId}')
// fetch('/api/eligibility?all=1&event_id=${eventId}')
// fetch('/api/rules?event_id=${eventId}')
// fetch('/api/rules/changes?event_id=${eventId}&limit=30')

<!-- Components inside AppProvider (majority): use useApp().eventId -->
<!-- Components outside AppProvider (RefereePortal, VolunteerPortal, RegisterPage): use useAuth().userRole.event_id or route params -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix components inside AppProvider — engine, schedule, checkin, incidents, refs, rules, settings, weather, payments, programs</name>
  <read_first>
    - components/engine/CommandCenter.tsx
    - components/engine/EngineTab.tsx
    - components/checkin/CheckInTab.tsx
    - components/incidents/IncidentsTab.tsx
    - components/schedule/ScheduleTab.tsx
    - components/refs/RefsTab.tsx
    - components/rules/RulesTab.tsx
    - components/settings/LeagueSettingsTab.tsx
    - components/weather/WeatherTab.tsx
    - components/payments/PaymentsTab.tsx
    - components/programs/ProgramApprovals.tsx
    - components/programs/ProgramDashboard.tsx
    - components/programs/RegistrationConfig.tsx
    - components/AppShell.tsx
    - components/auth/UserManagement.tsx
  </read_first>
  <files>
    components/engine/CommandCenter.tsx, components/engine/EngineTab.tsx,
    components/checkin/CheckInTab.tsx, components/incidents/IncidentsTab.tsx,
    components/schedule/ScheduleTab.tsx, components/refs/RefsTab.tsx,
    components/rules/RulesTab.tsx, components/settings/LeagueSettingsTab.tsx,
    components/weather/WeatherTab.tsx, components/payments/PaymentsTab.tsx,
    components/programs/ProgramApprovals.tsx, components/programs/ProgramDashboard.tsx,
    components/programs/RegistrationConfig.tsx,
    components/AppShell.tsx, components/auth/UserManagement.tsx
  </files>
  <action>
All these components are rendered inside `AppProvider` so they can use `useApp()`.

**General pattern for each component:**

1. Ensure `const { state, eventId, ... } = useApp()` is destructured (most already have `useApp()`)
2. Replace every `event_id: 1` with `event_id: eventId`
3. Replace every `.eq('event_id', 1)` with `.eq('event_id', eventId)`
4. Replace every `event_id=1` in fetch URLs with `event_id=${eventId}`
5. Replace every `?? 1` eventId fallback with direct usage (per D-02)

**File-specific changes:**

**CommandCenter.tsx** (4 hardcodes + 1 currentDate fallback):

- Line ~61: `const eventDateId = currentDate?.id ?? 1` — change to `const eventDateId = currentDate?.id; if (!eventDateId) return null` (inside render, not at top)
- Line ~65: `.eq('event_id', 1)` in ops_alerts query — change to `.eq('event_id', eventId)`
- Line ~83: `.eq('event_id', 1)` in ops_log query — change to `.eq('event_id', eventId)`
- Line ~812: `event_id: 1` in ops_log insert — change to `event_id: eventId`
- Line ~886: `.eq('event_id', 1)` in refresh query — change to `.eq('event_id', eventId)`
- Also: CommandCenter has its own Supabase subscriptions (`cmd-alerts`, `cmd-feed`, `cmd-games`) — add `filter: \`event_id=eq.${eventId}\``to each`.on('postgres_changes', ...)`config (SEC-05). Add`eventId` to those subscription useEffect dependency arrays.

**EngineTab.tsx** (1 hardcode):

- Line ~83: `event_id: 1` — change to `event_id: eventId`

**CheckInTab.tsx** (2 hardcodes + QR URL):

- Line ~59: `.upsert({ player_id: id, event_id: 1 }, ...)` — change `event_id: 1` to `event_id: eventId`
- Line ~191: `fetch('/api/eligibility?all=1&event_id=1')` — change to `fetch(\`/api/eligibility?all=1&event_id=${eventId}\`)`
- QR URL (D-05): Find where QR code URL is constructed. Change from any hardcoded event ID in URL to: `const eventSlug = state.event?.slug; const qrUrl = \`${window.location.origin}/checkin/${eventSlug}/${token}\``. Add guard: if `!eventSlug`, disable QR generation or return null.

**IncidentsTab.tsx** (2 hardcodes):

- Line ~125: `event_id: 1` in logIncident payload — change to `event_id: eventId`
- Line ~147: `event_id: 1` in dispatchTrainer payload — change to `event_id: eventId`

**ScheduleTab.tsx** (3 hardcodes):

- Line ~48: `fetch('/api/field-engine?event_id=1')` — change to `fetch(\`/api/field-engine?event_id=${eventId}\`)`
- Line ~177: `event_id: 1` in addGame payload — change to `event_id: eventId`
- Line ~575: `event_id: 1` in another payload — change to `event_id: eventId`

**RefsTab.tsx** (1 hardcode):

- Line ~612: `event_id: 1` — change to `event_id: eventId`

**RulesTab.tsx** (3 hardcodes):

- Line ~124: `fetch('/api/rules?event_id=1')` — change to `fetch(\`/api/rules?event_id=${eventId}\`)`
- Line ~140: `fetch('/api/rules/changes?event_id=1&limit=30')` — change to `fetch(\`/api/rules/changes?event_id=${eventId}&limit=30\`)`
- Line ~219: `JSON.stringify({ action: 'reset_all', event_id: 1 })` — change `event_id: 1` to `event_id: eventId`

**LeagueSettingsTab.tsx** (1 hardcode):

- Line ~139: `event_id: 1` in ops_log insert — change to `event_id: eventId`

**WeatherTab.tsx** (1 hardcode):

- Line ~194: `event_id: state.event?.id ?? 1` — change to `event_id: eventId` (use the definitive `eventId` from useApp, not `state.event?.id ?? 1`)

**PaymentsTab.tsx** (1 hardcode):

- Line ~358: `const eventId = state.event?.id ?? 1` — remove this line; use `eventId` from `useApp()` destructuring instead

**ProgramApprovals.tsx** (6 hardcodes):

- Lines ~108, 121, 140, 178, 195, 211: all `event_id: 1` — change each to `event_id: eventId`

**ProgramDashboard.tsx** (1 hardcode):

- Line ~124: `event_id: 1` — change to `event_id: eventId`

**RegistrationConfig.tsx** (4 hardcodes):

- Line ~61: `.eq('event_id', 1)` — change to `.eq('event_id', eventId)`
- Line ~65: `.eq('event_id', 1)` — change to `.eq('event_id', eventId)`
- Line ~94: `event_id: 1` — change to `event_id: eventId`
- Line ~155: `event_id: 1` — change to `event_id: eventId`

**AppShell.tsx** (1 hardcode):

- Line ~151: `<EventSetupTab eventId={(state.event as any)?.id ?? 1} />` — change to `<EventSetupTab eventId={eventId} />` where `eventId` comes from `useApp()`. Remove the `as any` cast entirely.

**UserManagement.tsx** (1 hardcode):

- Line ~27: `const eventId = (state.event as any)?.id ?? 1` — remove this line; use `eventId` from `useApp()` destructuring instead. Remove the `as any` cast.
  </action>
  <verify>
  <automated>grep -rn "event_id._: 1\b\|event_id=1\|event_id._?? 1\|as any._id._?? 1" components/engine/ components/checkin/ components/incidents/ components/schedule/ components/refs/ components/rules/ components/settings/ components/weather/ components/payments/ components/programs/ components/AppShell.tsx components/auth/UserManagement.tsx | grep -v "eventId\|event_id: eventId\|event_id=.\*eventId\|node_modules" || echo "PASS: No hardcoded event_id in AppProvider components"</automated>
  </verify>
  <acceptance_criteria> - Zero occurrences of literal `event_id: 1` in any of the 15 component files - Zero occurrences of `event_id=1` in fetch URLs in any component - Zero occurrences of `?? 1` as eventId fallback in any component - Zero occurrences of `(state.event as any)?.id ?? 1` in AppShell.tsx or UserManagement.tsx - `CommandCenter.tsx` subscription `.on()` configs include `filter` parameter - `CheckInTab.tsx` QR URL uses `state.event?.slug` not a numeric event ID - `WeatherTab.tsx` uses `event_id: eventId` not `state.event?.id ?? 1` - `PaymentsTab.tsx` uses `eventId` from `useApp()` not `state.event?.id ?? 1`
  </acceptance_criteria>
  <done>All 15 components inside AppProvider use dynamic eventId from useApp(). No hardcoded event_id: 1 remains.</done>
  </task>

<task type="auto">
  <name>Task 2: Fix portal and registration components outside AppProvider</name>
  <read_first>
    - components/auth/RefereePortal.tsx
    - components/auth/VolunteerPortal.tsx
    - components/auth/RegisterPage.tsx
    - types/index.ts
    - lib/auth.tsx
  </read_first>
  <files>components/auth/RefereePortal.tsx, components/auth/VolunteerPortal.tsx, components/auth/RegisterPage.tsx, types/index.ts</files>
  <action>
These components are rendered OUTSIDE AppProvider (from app/page.tsx before event selection). Calling `useApp()` would throw "useApp must be used within AppProvider". Instead, derive eventId from `useAuth().userRole.event_id`.

**Step 0: Verify/fix UserRole type in types/index.ts:**
Read `types/index.ts` and check the `UserRole` interface. If `event_id` is not a field, add it:

```typescript
export interface UserRole {
  // ... existing fields ...
  event_id: number // ← ensure this exists
}
```

Also verify in `lib/auth.tsx` that the `user_roles` query SELECT includes `event_id` (or uses `*`).

**RefereePortal.tsx** (3 hardcodes):

1. At top of component, after `useAuth()`: `const portalEventId = userRole?.event_id`
2. Add null guard: `if (!portalEventId) return null` (per D-01 — no spinner)
3. Line ~139: `event_id: 1` — change to `event_id: portalEventId`
4. Line ~143: `event_id: 1` — change to `event_id: portalEventId`
5. Line ~424: `fetch('/api/eligibility?all=1&event_id=1')` — change to `fetch(\`/api/eligibility?all=1&event_id=${portalEventId}\`)`

**VolunteerPortal.tsx** (3 hardcodes):

1. At top of component, after `useAuth()`: `const portalEventId = userRole?.event_id`
2. Add null guard: `if (!portalEventId) return null`
3. Line ~132: `event_id: 1` — change to `event_id: portalEventId`
4. Line ~136: `event_id: 1` — change to `event_id: portalEventId`
5. Line ~397: `fetch('/api/eligibility?all=1&event_id=1')` — change to `fetch(\`/api/eligibility?all=1&event_id=${portalEventId}\`)`

**RegisterPage.tsx** (5 hardcodes):
RegisterPage is at `app/register/page.tsx` or rendered from it — it's outside AppProvider. Read the file to determine how it gets its event context. Likely options:

- URL query param `?event_id=X` or route param
- Props passed from a parent page

Wherever the event_id source is, replace the hardcodes:

- Line ~133: `.eq('event_id', 1)` — change to `.eq('event_id', registrationEventId)` (local variable sourced from URL/props)
- Line ~139: `.eq('event_id', 1)` — change to `.eq('event_id', registrationEventId)`
- Line ~460: `event_id: 1` — change to `event_id: registrationEventId`
- Line ~472: `event_id: 1` — change to `event_id: registrationEventId`
- Line ~499: `event_id: 1` — change to `event_id: registrationEventId`

If RegisterPage receives event context from a URL/query param, extract it at the top: `const registrationEventId = Number(searchParams.get('event_id'))`. Add guard: `if (!registrationEventId) return <div>Event not found</div>`.

If RegisterPage receives event context from props or from a route like `/register/[slug]`, derive it from the route params or props accordingly.

After all portal/register fixes, run the full verification suite.
</action>
<verify>
<automated>grep -rn "event_id._: 1\b\|event_id=1\|event_id._?? 1" components/auth/RefereePortal.tsx components/auth/VolunteerPortal.tsx components/auth/RegisterPage.tsx | grep -v "eventId\|portalEventId\|registrationEventId\|event_id: portalEventId\|event_id: registrationEventId" || echo "PASS: No hardcoded event_id in portal/register components" && npm run type-check && npm run test</automated>
</verify>
<acceptance_criteria> - `types/index.ts` UserRole interface includes `event_id: number` - `components/auth/RefereePortal.tsx` contains `const portalEventId = userRole?.event_id` - `components/auth/RefereePortal.tsx` contains zero occurrences of literal `event_id: 1` - `components/auth/VolunteerPortal.tsx` contains `const portalEventId = userRole?.event_id` - `components/auth/VolunteerPortal.tsx` contains zero occurrences of literal `event_id: 1` - `components/auth/RegisterPage.tsx` contains zero occurrences of literal `event_id: 1` or `.eq('event_id', 1)` - `npm run type-check` passes - `npm run test` passes
</acceptance_criteria>
<done>All portal and registration components use dynamic eventId from their respective sources. No hardcoded event_id remains anywhere in the components/ directory.</done>
</task>

<task type="auto">
  <name>Task 3: Final grep verification — zero hardcoded event_id across entire codebase</name>
  <read_first>
    - .planning/phases/02-hardcode-removal-event-context/02-CONTEXT.md
  </read_first>
  <files></files>
  <action>
Run comprehensive grep to verify zero hardcoded event_id references remain across all source code (lib/, components/, app/api/).

**Verification commands:**

1. `grep -rn "const EVENT_ID = 1" lib/ components/ app/` — must return 0 results
2. `grep -rn "event_id: 1[^0-9]" lib/ components/ app/api/` — must return 0 results (excluding test files and node_modules)
3. `grep -rn "event_id=1[^0-9]" lib/ components/ app/api/` — must return 0 results (URL params)
4. `grep -rn "'event_id', 1)" lib/ components/ app/api/` — must return 0 results (.eq calls)
5. `grep -rn "?? '1'" app/api/` — must return 0 results (searchParams fallbacks)
6. `grep -rn "?? 1" lib/ components/ app/api/ | grep -i "event"` — must return 0 results (eventId fallbacks)
7. `grep -rn "as any.*id.*?? 1" components/` — must return 0 results (cast pattern)

If ANY grep returns results, fix the remaining occurrence in the appropriate file before completing.

Also run:

- `npm run type-check` — must pass
- `npm run test` — must pass
- `npm run lint` — check for exhaustive-deps warnings

This task produces no file changes if all prior tasks were complete. It exists as the phase gate verification.
</action>
<verify>
<automated>grep -rn "EVENT_ID = 1\|event_id: 1[^0-9]\|event_id=1[^0-9]\|'event_id', 1)\|?? '1'" lib/ components/ app/api/ | grep -v node_modules | grep -v **tests** | grep -v ".test." || echo "PASS: Zero hardcoded event_id in codebase" && npm run type-check && npm run test</automated>
</verify>
<acceptance_criteria> - grep for `EVENT_ID = 1` across lib/, components/, app/api/ returns zero matches - grep for `event_id: 1` (literal) across lib/, components/, app/api/ returns zero matches - grep for `?? '1'` across app/api/ returns zero matches - grep for `event_id ?? 1` across lib/, components/, app/ returns zero matches - `npm run type-check` exits 0 - `npm run test` exits 0
</acceptance_criteria>
<done>Phase 2 complete. Zero hardcoded event_id references remain in engines, API routes, store, or components. SEC-04 and SEC-05 fully satisfied.</done>
</task>

</tasks>

<verification>
1. Full codebase grep: `grep -rn "event_id.*: 1\b\|EVENT_ID.*= 1\|?? .1.\|event_id=1" lib/ components/ app/api/ | grep -v "eventId\|portalEventId\|registrationEventId\|node_modules\|__tests__"` returns 0 results
2. `npm run type-check` passes
3. `npm run test` passes
4. `npm run lint` passes with no exhaustive-deps warnings on store.tsx
</verification>

<success_criteria>

- Zero hardcoded event_id = 1 references in the entire codebase (lib/, components/, app/api/)
- All components inside AppProvider use useApp().eventId
- Portal components use useAuth().userRole.event_id
- QR code URLs use event slug per D-05
- CommandCenter subscriptions include event_id filter per SEC-05
- Type-check, test suite, and lint all pass
  </success_criteria>

<output>
After completion, create `.planning/phases/02-hardcode-removal-event-context/02-04-SUMMARY.md`
</output>
